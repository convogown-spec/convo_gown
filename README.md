# Gownify (Convo Gown)

Frontend React app for Gownify — graduation gown rentals.

Local setup:

```bash
npm install
npm start
```

To create a remote GitHub repository and push:

```bash
git remote add origin git@github.com:<username>/gownify.git
git branch -M main
git push -u origin main
```
